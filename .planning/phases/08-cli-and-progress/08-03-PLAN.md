---
phase: 08-cli-and-progress
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - crates/ath-cli/src/run.rs
  - crates/ath-cli/src/progress.rs
  - crates/ath-cli/src/verbose.rs
  - crates/ath-orchestrator/src/progress.rs
  - crates/ath-orchestrator/src/phase_runner.rs
  - crates/ath-orchestrator/src/review.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "`--verbose` preserves the normal progress UI and adds grouped transcript blocks after each task/review unit completes"
    - "Verbose mode includes executor prompts/responses, reviewer prompts/verdicts, and retry feedback"
    - "Secrets and configured API keys are redacted before any transcript text is printed"
    - "Default output stays calm when `--verbose` is false"
  artifacts:
    - path: "crates/ath-cli/src/verbose.rs"
      provides: "Transcript grouping and redaction helpers for verbose mode"
      contains: "redact"
    - path: "crates/ath-orchestrator/src/progress.rs"
      provides: "Verbose/transcript event payloads or hooks layered on the same progress seam"
      contains: "Transcript"
    - path: "crates/ath-cli/src/run.rs"
      provides: "Verbose sink wiring on top of the normal execution path"
      contains: "verbose"
  key_links:
    - from: "crates/ath-orchestrator/src/phase_runner.rs"
      to: "crates/ath-orchestrator/src/progress.rs"
      via: "executor/reviewer prompt-response data is emitted through the same event seam"
      pattern: "Transcript"
    - from: "crates/ath-cli/src/run.rs"
      to: "crates/ath-cli/src/verbose.rs"
      via: "run path attaches transcript sink when global verbose is enabled"
      pattern: "verbose::"
---

<objective>
Layer full verbose transcript visibility onto the default execution path without destroying the normal progress experience.

Purpose: The roadmap requires `ath run --verbose` to show full agent prompt/response transcripts, but the user explicitly wants those transcripts grouped and secondary to the default live board.

Output: Verbose transcript events, redaction helpers, and grouped transcript rendering on top of the existing progress/event pipeline.
</objective>

<execution_context>
@C:/Users/Letta/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Letta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cli-and-progress/08-CONTEXT.md
@.planning/phases/08-cli-and-progress/08-RESEARCH.md
@.planning/phases/08-cli-and-progress/08-VALIDATION.md
@.planning/phases/08-cli-and-progress/08-02-PLAN.md

<interfaces>
From crates/ath-orchestrator/src/phase_runner.rs:
```rust
pub struct TaskOutput {
    pub task_name: String,
    pub agent: AgentKind,
    pub files_produced: Vec<FileOutput>,
    pub explanation: String,
    pub issues_encountered: Vec<String>,
    pub input_tokens: u64,
    pub output_tokens: u64,
}
```

From crates/ath-types/src/phase.rs:
```rust
pub struct ReviewAttempt {
    pub attempt_number: u32,
    pub verdict: ReviewVerdict,
    pub timestamp: DateTime<Utc>,
}
```

From Phase 8 context:
- Verbose mode prints grouped transcript blocks after task completion
- Verbose mode includes executor, reviewer, and retry feedback transcripts
- Secrets must always be redacted
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the progress seam to carry transcript data for verbose mode</name>
  <files>crates/ath-orchestrator/src/progress.rs, crates/ath-orchestrator/src/phase_runner.rs, crates/ath-orchestrator/src/review.rs</files>
  <behavior>
    - Test 1: Task execution can emit prompt/response transcript payloads when verbose capture is enabled
    - Test 2: Review dispatch can emit reviewer prompt/verdict transcript payloads
    - Test 3: Retry feedback injection is exposed as transcript context for the next attempt
    - Test 4: Normal mode can still run without transcript payload handling enabled
  </behavior>
  <action>
    1. Extend the Phase 8 progress/event seam with transcript-oriented payloads or a companion callback so executor and reviewer exchanges can be captured without changing the normal run flow.
    2. Emit transcript events from task dispatch and review dispatch points, including:
       - agent identity
       - task/phase label
       - prompt text
       - response/verdict text
       - retry feedback context when applicable
    3. Keep transcript capture opt-in so normal mode does not pay for unnecessary formatting work.
  </action>
  <verify>
    <automated>cargo test -p ath-orchestrator verbose::tests</automated>
  </verify>
  <done>The orchestrator can expose full prompt/response/retry transcript data without forking the execution path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render grouped verbose transcript blocks with redaction in the CLI</name>
  <files>crates/ath-cli/src/run.rs, crates/ath-cli/src/progress.rs, crates/ath-cli/src/verbose.rs</files>
  <behavior>
    - Test 1: Verbose mode still renders the normal progress reporter output
    - Test 2: Completed tasks flush grouped transcript blocks instead of live interleaved prompt spam
    - Test 3: Reviewer verdicts and retry feedback appear in the grouped output
    - Test 4: API keys and obvious secrets are redacted before rendering
  </behavior>
  <action>
    1. Create `crates/ath-cli/src/verbose.rs` with:
       - transcript block formatter
       - redaction helpers for provider keys and common secret patterns
       - stable labels for executor vs reviewer vs retry-feedback blocks
    2. Update `run::run_command` so `--verbose` attaches the transcript sink in addition to the default progress reporter.
    3. Ensure transcripts print after each task/review unit completes, never inline with the live progress bar redraw path.
    4. Keep the non-verbose path unchanged.
  </action>
  <verify>
    <automated>cargo test -p ath-cli verbose::tests</automated>
  </verify>
  <done>Verbose mode shows grouped, redacted end-to-end transcripts while preserving the normal progress UX.</done>
</task>

</tasks>

<verification>
- `cargo test -p ath-orchestrator verbose::tests`
- `cargo test -p ath-cli verbose::tests`
- `cargo test -p ath-cli`
- `cargo test --workspace`
</verification>

<success_criteria>
- `--verbose` includes executor, reviewer, and retry-feedback transcripts
- Transcript output is grouped after completion units rather than streamed live
- Secrets are redacted
- Default output is unchanged when verbose mode is off
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-and-progress/08-03-SUMMARY.md`
</output>
