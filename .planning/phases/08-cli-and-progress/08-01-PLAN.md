---
phase: 08-cli-and-progress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ath-cli/src/main.rs
  - crates/ath-cli/src/run.rs
  - crates/ath-cli/src/init.rs
  - crates/ath-cli/src/report.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Plain `ath` with no subcommand shows real help output instead of a custom one-line message"
    - "`ath run`, `ath init`, and `ath report` each have distinct clap surfaces and `--help` output"
    - "`ath run` parses a `--dry-run` flag even before the dry-run execution path is implemented"
    - "Command handling is split into focused, testable module functions instead of one large inline match"
  artifacts:
    - path: "crates/ath-cli/src/main.rs"
      provides: "CLI root that delegates to run/init/report modules and prints help when no subcommand is given"
      contains: "Cli::command"
    - path: "crates/ath-cli/src/run.rs"
      provides: "RunArgs and run command entry point for future execution wiring"
      contains: "RunArgs"
    - path: "crates/ath-cli/src/report.rs"
      provides: "Report command surface with optional explicit target and latest-run default semantics"
      contains: "report_command"
  key_links:
    - from: "crates/ath-cli/src/main.rs"
      to: "crates/ath-cli/src/run.rs"
      via: "match dispatch calls run::run_command"
      pattern: "run::run_command"
    - from: "crates/ath-cli/src/main.rs"
      to: "crates/ath-cli/src/report.rs"
      via: "match dispatch calls report::report_command"
      pattern: "report::report_command"
---

<objective>
Refactor the CLI surface so `ath`, `ath run`, `ath init`, and `ath report` have a stable, testable command shape that matches the locked Phase 8 UX decisions.

Purpose: This plan turns the current placeholder `main.rs` into a command-dispatch entry point the later progress, verbose, and dry-run plans can build on.

Output: `ath-cli` command modules for run/init/report, help-first no-subcommand behavior, and parsing coverage for the expanded clap surface.
</objective>

<execution_context>
@C:/Users/Letta/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Letta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cli-and-progress/08-CONTEXT.md
@.planning/phases/08-cli-and-progress/08-RESEARCH.md
@.planning/phases/08-cli-and-progress/08-VALIDATION.md

<interfaces>
From crates/ath-cli/src/main.rs:
```rust
#[derive(Parser)]
struct Cli {
    #[command(subcommand)]
    command: Option<Commands>,
    #[arg(long, global = true)]
    verbose: bool,
    #[arg(long, global = true)]
    no_color: bool,
}

#[derive(Subcommand)]
enum Commands {
    Run { description: Option<String>, spec: Option<PathBuf>, codebase: Option<PathBuf> },
    Init,
    Report,
}
```

From Phase 8 context:
- Plain `ath` with no subcommand should show help
- `ath report` defaults to latest run when no explicit target is given
- `ath init` remains interactive and should later offer example-spec generation during setup
- `ath run --dry-run` must exist as a real flag on the run surface
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split the CLI into command modules with an explicit run surface</name>
  <files>crates/ath-cli/src/main.rs, crates/ath-cli/src/run.rs, crates/ath-cli/src/init.rs, crates/ath-cli/src/report.rs</files>
  <behavior>
    - Test 1: `Cli::command().debug_assert()` passes with the new command structure
    - Test 2: `ath` with no subcommand prints clap help/usage instead of the custom fallback string
    - Test 3: `ath run --help` shows description/spec/codebase plus the new `--dry-run` option
    - Test 4: `ath init --help` and `ath report --help` each describe their own command purpose
    - Test 5: `Cli::try_parse_from(["ath", "run", "--dry-run"])` succeeds and sets `dry_run = true`
  </behavior>
  <action>
    1. Create `crates/ath-cli/src/run.rs`, `init.rs`, and `report.rs`, each with a focused command entry point (`run_command`, `init_command`, `report_command`) returning `anyhow::Result<()>`.
    2. Move clap argument definitions out of the monolithic `Commands` enum fields into explicit argument structs where helpful (`RunArgs`, optional `ReportArgs` target, and any shared global args adapter needed by the module functions).
    3. Add `--dry-run` to the run command surface now, but leave execution behavior for Plan 04.
    4. Change the no-subcommand path so plain `ath` prints clap help output. Use clap's command factory or equivalent help-printing path; do not leave a one-line "Use --help" string.
    5. Keep `--verbose` and `--no-color` as global flags.
  </action>
  <verify>
    <automated>cargo test -p ath-cli cli_surface::tests</automated>
  </verify>
  <done>The CLI surface is module-based, help-first, and `ath run` now exposes the dry-run flag required by Phase 8.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add minimal command handlers that preserve later-phase intent</name>
  <files>crates/ath-cli/src/main.rs, crates/ath-cli/src/init.rs, crates/ath-cli/src/report.rs, crates/ath-cli/src/run.rs</files>
  <behavior>
    - Test 1: `report_command(None)` chooses a latest-run default path instead of requiring a target
    - Test 2: `init_command()` returns a human-readable interactive-setup placeholder rather than "not yet implemented"
    - Test 3: `report_command()` returns a human-readable report placeholder rather than "not yet implemented"
    - Test 4: `run_command()` still preserves the current parse/decompose flow when `--dry-run` is false
  </behavior>
  <action>
    1. Replace the inline `println!("Init not yet implemented.")` and `println!("Report not yet implemented.")` branches with module functions that describe the future interactive/setup and latest-run behavior in user-facing text.
    2. Keep the existing `run` path working by moving its current parse/decompose flow into `run::run_command`.
    3. Ensure `report_command` accepts an optional explicit target, but defaults internally to a latest-run lookup contract when the target is `None`.
    4. Add unit tests around the module entry points instead of relying only on subprocess-level assertions.
  </action>
  <verify>
    <automated>cargo test -p ath-cli cli_surface::tests</automated>
  </verify>
  <done>`ath-cli` has stable command handlers and no longer relies on hard-coded placeholder strings in `main.rs`.</done>
</task>

</tasks>

<verification>
- `cargo test -p ath-cli cli_surface::tests`
- `cargo test -p ath-cli`
- `cargo test --workspace`
</verification>

<success_criteria>
- Plain `ath` prints help output
- `ath run`, `ath init`, and `ath report` each have their own command surface and help text
- `ath run --dry-run` parses successfully
- `main.rs` becomes a dispatcher, not a monolithic command implementation
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-and-progress/08-01-SUMMARY.md`
</output>
