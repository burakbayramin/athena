---
phase: 08-cli-and-progress
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - crates/ath-cli/src/main.rs
  - crates/ath-cli/src/run.rs
  - crates/ath-cli/src/dry_run.rs
  - crates/ath-planner/src/decompose/display.rs
autonomous: true
requirements: [PLAN-05]

must_haves:
  truths:
    - "`ath run --dry-run` exits before any LLM or git call"
    - "Dry-run succeeds only when a local serialized plan artifact exists, and fails clearly when it does not"
    - "Dry-run output shows phases, task ordering, assigned agents, dependencies, and parallel groups"
    - "Normal non-dry-run runs persist or refresh the local plan artifact used by future dry-runs"
  artifacts:
    - path: "crates/ath-cli/src/dry_run.rs"
      provides: "Local plan-cache loading/writing and dry-run-only command path"
      contains: "last-plan.json"
    - path: "crates/ath-planner/src/decompose/display.rs"
      provides: "Plan display that can render assigned agents when present"
      contains: "assigned_agent"
    - path: "crates/ath-cli/src/run.rs"
      provides: "Early dry-run branch plus plan-cache write after routing and isolation"
      contains: "dry_run"
  key_links:
    - from: "crates/ath-cli/src/run.rs"
      to: "crates/ath-cli/src/dry_run.rs"
      via: "run command branches to dry-run before any agent or git execution"
      pattern: "dry_run::"
    - from: "crates/ath-cli/src/dry_run.rs"
      to: "crates/ath-planner/src/decompose/display.rs"
      via: "serialized ExecutionPlan is rendered through the existing plan display path"
      pattern: "display_execution_plan"
    - from: "crates/ath-cli/src/run.rs"
      to: "crates/ath-orchestrator/src/router.rs"
      via: "assigned-agent plan is cached only after routing"
      pattern: "assign_all_tasks"
---

<objective>
Implement an honest dry-run path that never spends API or git work, while still printing the full routed execution plan when local planning data exists.

Purpose: `PLAN-05` is only satisfied if `--dry-run` is cost-free. Since the current planning path is LLM-backed, dry-run needs a local serialized plan artifact and a clear failure mode when that artifact is absent.

Output: `dry_run.rs`, local serialized plan-cache helpers, assigned-agent-aware plan display, and tests proving no agent/git execution occurs in dry-run mode.
</objective>

<execution_context>
@C:/Users/Letta/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Letta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cli-and-progress/08-CONTEXT.md
@.planning/phases/08-cli-and-progress/08-RESEARCH.md
@.planning/phases/08-cli-and-progress/08-VALIDATION.md
@.planning/phases/08-cli-and-progress/08-02-PLAN.md
@.planning/phases/08-cli-and-progress/08-03-PLAN.md

<interfaces>
From crates/ath-types/src/plan.rs:
```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ExecutionPlan {
    pub phases: Vec<PhaseSpec>,
    pub execution_order: Vec<u32>,
    pub parallel_groups: Vec<Vec<u32>>,
    pub critical_path_length: usize,
}
```

From crates/ath-planner/src/decompose/display.rs:
```rust
pub fn display_execution_plan(plan: &ExecutionPlan, warnings: &[PlanWarning]);
pub fn format_execution_plan(
    plan: &ExecutionPlan,
    warnings: &[PlanWarning],
    out: &mut impl std::fmt::Write,
) -> std::fmt::Result;
```

From Phase 8 context:
- Dry-run must fail clearly when no local plan exists
- When dry-run succeeds, it must show the full plan including assigned agents
- No LLM or git call may happen in the dry-run path
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add local plan-cache helpers and persist routed plans for future dry-runs</name>
  <files>crates/ath-cli/src/run.rs, crates/ath-cli/src/dry_run.rs</files>
  <behavior>
    - Test 1: A routed `ExecutionPlan` can be serialized and written to a project-local cache file
    - Test 2: Loading the cache round-trips the same `ExecutionPlan`
    - Test 3: Normal non-dry-run runs refresh the local cache after routing/isolation but before execution
  </behavior>
  <action>
    1. Create `crates/ath-cli/src/dry_run.rs` with helpers to read and write a project-local plan cache. Use a stable project-local path such as `.ath/last-plan.json` so the success path is explicit and deterministic.
    2. Update `run::run_command` so the normal execution path writes the routed, isolation-checked `ExecutionPlan` to that cache before handing off to `AgentCoordinator`.
    3. Keep the cache format small and testable: serialized `ExecutionPlan` plus any metadata strictly needed to render the plan.
  </action>
  <verify>
    <automated>cargo test -p ath-cli dry_run::tests::plan_cache_round_trip</automated>
  </verify>
  <done>A routed local plan artifact exists and can be reused by future dry-runs without recomputing the plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Early dry-run branch with assigned-agent-aware plan display</name>
  <files>crates/ath-cli/src/main.rs, crates/ath-cli/src/run.rs, crates/ath-cli/src/dry_run.rs, crates/ath-planner/src/decompose/display.rs</files>
  <behavior>
    - Test 1: `ath run --dry-run` with no local plan fails clearly and does not construct execution backends
    - Test 2: `ath run --dry-run` with a cached local plan prints the full plan and exits before execution
    - Test 3: Dry-run output includes assigned agents when they are present on tasks
    - Test 4: No git commit path is reached in dry-run mode
  </behavior>
  <action>
    1. Add an early dry-run branch in `run::run_command` that executes before any LLM-backed parsing/decomposition or git-backed execution path.
    2. Load the local plan cache; if missing, return a clear actionable error explaining that dry-run requires a previously generated local plan.
    3. Extend `crates/ath-planner/src/decompose/display.rs` so plan display can show assigned agents when the routed plan includes them. Keep the existing formatting path reusable rather than creating a second formatter from scratch.
    4. Add tests that use panic/failing backends or equivalent guards to prove no agent send/git execution occurs in the dry-run branch.
  </action>
  <verify>
    <automated>cargo test -p ath-cli dry_run::tests && cargo test -p ath-planner decompose::display::tests</automated>
  </verify>
  <done>`ath run --dry-run` is honest, cost-free, and prints the full routed plan when a local plan cache exists.</done>
</task>

</tasks>

<verification>
- `cargo test -p ath-cli dry_run::tests`
- `cargo test -p ath-planner decompose::display::tests`
- `cargo test -p ath-cli`
- `cargo test --workspace`
</verification>

<success_criteria>
- Dry-run never reaches agent or git execution
- Missing local plan cache produces a clear failure message
- Cached plan dry-runs print assigned agents as part of the full plan view
- Normal runs refresh the local cache used by dry-run
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-and-progress/08-04-SUMMARY.md`
</output>
