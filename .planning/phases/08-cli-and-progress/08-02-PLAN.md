---
phase: 08-cli-and-progress
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - Cargo.toml
  - crates/ath-cli/Cargo.toml
  - crates/ath-cli/src/run.rs
  - crates/ath-cli/src/progress.rs
  - crates/ath-cli/src/main.rs
  - crates/ath-orchestrator/src/lib.rs
  - crates/ath-orchestrator/src/progress.rs
  - crates/ath-orchestrator/src/coordinator.rs
  - crates/ath-orchestrator/src/phase_runner.rs
autonomous: true
requirements: [OUTP-02]

must_haves:
  truths:
    - "`ath run` executes through routing, isolation, and AgentCoordinator instead of stopping after plan display"
    - "Progress events are emitted while work is happening, not only after a phase finishes"
    - "Default run output shows current phase, overall phase position, active task, and active agent"
    - "Review failures and retries are preserved as durable terminal messages outside the transient live board"
  artifacts:
    - path: "crates/ath-orchestrator/src/progress.rs"
      provides: "ProgressEvent definitions and observer/callback seam for real-time execution updates"
      contains: "ProgressEvent"
    - path: "crates/ath-cli/src/progress.rs"
      provides: "Terminal progress reporter implementing the hybrid board UX"
      contains: "TerminalProgressReporter"
    - path: "crates/ath-cli/src/run.rs"
      provides: "End-to-end run pipeline that routes tasks, checks isolation, and executes via AgentCoordinator"
      contains: "assign_all_tasks"
  key_links:
    - from: "crates/ath-cli/src/run.rs"
      to: "crates/ath-orchestrator/src/router.rs"
      via: "run path assigns agents before execution"
      pattern: "assign_all_tasks"
    - from: "crates/ath-cli/src/run.rs"
      to: "crates/ath-orchestrator/src/coordinator.rs"
      via: "run path executes the routed plan through AgentCoordinator"
      pattern: "run_plan"
    - from: "crates/ath-orchestrator/src/phase_runner.rs"
      to: "crates/ath-orchestrator/src/progress.rs"
      via: "phase runner emits task/review/retry lifecycle events"
      pattern: "ProgressEvent"
---

<objective>
Wire the default `ath run` path into the real execution engine and add real-time terminal progress reporting that satisfies `OUTP-02`.

Purpose: Phase 7 built the execution engine, but the CLI still stops after plan display. This plan makes Athena actually run work while keeping the terminal informative instead of silent.

Output: A real `ath run` execution path with progress events from the orchestrator and a default hybrid terminal reporter in `ath-cli`.
</objective>

<execution_context>
@C:/Users/Letta/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Letta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cli-and-progress/08-CONTEXT.md
@.planning/phases/08-cli-and-progress/08-RESEARCH.md
@.planning/phases/08-cli-and-progress/08-VALIDATION.md
@.planning/phases/08-cli-and-progress/08-01-PLAN.md

<interfaces>
From crates/ath-orchestrator/src/router.rs:
```rust
pub fn assign_all_tasks(
    phase: &mut PhaseSpec,
    table: &HashMap<String, AgentKind>,
    available: impl Fn(&AgentKind) -> bool,
) -> Result<Vec<RoutingDecision>, IsolationError>;
```

From crates/ath-orchestrator/src/isolation.rs:
```rust
pub fn check_isolation(plan: &ExecutionPlan) -> Result<(), IsolationError>;
```

From crates/ath-orchestrator/src/coordinator.rs:
```rust
pub async fn run_plan(&self, plan: &ExecutionPlan) -> Result<Vec<PhaseRecord>, PhaseRunnerError>;
```

From crates/ath-orchestrator/src/phase_runner.rs:
```rust
pub enum PhaseStatus {
    Pending,
    Running,
    AwaitingReview,
    Complete,
    ReviewFailed { attempts: u32, final_reason: String },
    Retrying { attempt_number: u32 },
}
```

From Phase 8 context:
- Default output is a hybrid live board
- Show both current phase and overall progress
- Show active task + active agent
- Keep retry/review milestones as durable messages
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add orchestrator-side progress events and emit them through the execution lifecycle</name>
  <files>crates/ath-orchestrator/src/lib.rs, crates/ath-orchestrator/src/progress.rs, crates/ath-orchestrator/src/coordinator.rs, crates/ath-orchestrator/src/phase_runner.rs</files>
  <behavior>
    - Test 1: Phase execution emits `PhaseStarted` before any task event
    - Test 2: Each task emits `TaskStarted` then `TaskCompleted` with the assigned agent
    - Test 3: Review dispatch emits a review-start event before reviewer output is parsed
    - Test 4: Review failure emits a durable retry/review-failed event with attempt number and reason summary
    - Test 5: Successful completion emits `PhaseCompleted`
  </behavior>
  <action>
    1. Create `crates/ath-orchestrator/src/progress.rs` with a project-owned `ProgressEvent` enum plus a small observer/callback seam that the CLI can implement without importing terminal libraries into the orchestrator crate.
    2. Update `lib.rs` to export the new progress module.
    3. Extend `AgentCoordinator::run_plan()` and `run_phase()` to accept an optional observer/callback and emit phase/task/review/retry lifecycle events as work happens.
    4. Keep the event payloads semantic and testable: phase id/name, task name, assigned agent, attempt number, reviewer, and reason summary where relevant.
    5. Do not add `indicatif` or presentation formatting to `ath-orchestrator`; keep that in `ath-cli`.
  </action>
  <verify>
    <automated>cargo test -p ath-orchestrator progress::tests</automated>
  </verify>
  <done>Orchestrator code emits stable progress events throughout execution, and those events are covered by focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the default terminal reporter and wire `ath run` into real execution</name>
  <files>Cargo.toml, crates/ath-cli/Cargo.toml, crates/ath-cli/src/main.rs, crates/ath-cli/src/run.rs, crates/ath-cli/src/progress.rs</files>
  <behavior>
    - Test 1: `ath run` routes tasks and checks isolation before executing the plan
    - Test 2: The default reporter renders current phase, overall progress, active task, and active agent from emitted events
    - Test 3: Review failures and retries are printed as durable milestone lines
    - Test 4: Non-interactive / `NO_COLOR` mode falls back to plain text progress without spinner garbage
  </behavior>
  <action>
    1. Add the progress stack chosen in research to workspace/CLI manifests:
       - root `Cargo.toml`: `indicatif`, `tracing`, `tracing-subscriber`, `tracing-indicatif`
       - `crates/ath-cli/Cargo.toml`: add `ath-orchestrator` and `ath-git` path dependencies plus any progress crates needed directly by the CLI
    2. Create `crates/ath-cli/src/progress.rs` with `TerminalProgressReporter` implementing the locked hybrid board:
       - current phase + overall phase position
       - short task checklist with pending/running/done states
       - durable milestone lines for review start/fail/retry/phase complete
       - plain-text fallback when the terminal is not interactive
    3. Update `run::run_command` to perform the real pipeline:
       - resolve input mode and parse to `ProjectSpec`
       - decompose to `ExecutionPlan`
       - assign agents with `assign_all_tasks`
       - run `check_isolation`
       - build provider registry
       - execute through `AgentCoordinator` with the progress reporter attached
    4. Preserve current help/color semantics and keep the progress reporter in the CLI crate.
  </action>
  <verify>
    <automated>cargo test -p ath-cli progress::tests && cargo test -p ath-orchestrator progress::tests</automated>
  </verify>
  <done>`ath run` now performs real execution with a default live board that satisfies `OUTP-02` and no longer leaves the user staring at a silent process.</done>
</task>

</tasks>

<verification>
- `cargo test -p ath-orchestrator progress::tests`
- `cargo test -p ath-cli progress::tests`
- `cargo test -p ath-cli`
- `cargo test --workspace`
</verification>

<success_criteria>
- Real-time progress events exist throughout execution
- Default output shows current phase, overall progress, task status, and agent identity
- Retry/review milestones remain visible as durable lines
- `ath run` executes through routing, isolation, and coordinator wiring
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-and-progress/08-02-SUMMARY.md`
</output>
