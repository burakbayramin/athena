---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/ath-types/Cargo.toml
  - crates/ath-types/src/lib.rs
  - crates/ath-config/Cargo.toml
  - crates/ath-config/src/lib.rs
  - crates/ath-cli/Cargo.toml
  - crates/ath-cli/src/main.rs
  - crates/ath-agents/Cargo.toml
  - crates/ath-agents/src/lib.rs
  - crates/ath-git/Cargo.toml
  - crates/ath-git/src/lib.rs
  - crates/ath-planner/Cargo.toml
  - crates/ath-planner/src/lib.rs
  - crates/ath-orchestrator/Cargo.toml
  - crates/ath-orchestrator/src/lib.rs
  - .gitignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "`cargo check --workspace` compiles without errors"
    - "All 7 crates exist in the workspace with correct inter-crate dependencies"
    - "Workspace-level dependency management is configured in root Cargo.toml"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace root with [workspace.dependencies]"
      contains: "[workspace]"
    - path: "crates/ath-types/Cargo.toml"
      provides: "Types crate manifest"
      contains: "ath-types"
    - path: "crates/ath-config/Cargo.toml"
      provides: "Config crate manifest with ath-types dependency"
      contains: "ath-types"
    - path: "crates/ath-cli/Cargo.toml"
      provides: "CLI binary crate manifest"
      contains: "ath-config"
  key_links:
    - from: "crates/ath-config/Cargo.toml"
      to: "crates/ath-types"
      via: "path dependency"
      pattern: 'ath-types.*path.*=.*"../ath-types"'
    - from: "crates/ath-cli/Cargo.toml"
      to: "crates/ath-config"
      via: "path dependency"
      pattern: 'ath-config.*path.*=.*"../ath-config"'
---

<objective>
Create the Cargo workspace scaffold with all 7 ath-* crates, workspace-level dependency management, and minimal compilable stubs for each crate.

Purpose: Every subsequent plan needs a compilable workspace to build against. This establishes the crate layout, dependency graph, and shared dependency versions that all Phase 1 plans (and all future phases) depend on.

Output: A `cargo check --workspace` passing workspace with 7 crates and a binary target named `ath`.
</objective>

<execution_context>
@C:/Users/master/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workspace root and all crate manifests</name>
  <files>
    Cargo.toml,
    .gitignore,
    crates/ath-types/Cargo.toml,
    crates/ath-config/Cargo.toml,
    crates/ath-cli/Cargo.toml,
    crates/ath-agents/Cargo.toml,
    crates/ath-git/Cargo.toml,
    crates/ath-planner/Cargo.toml,
    crates/ath-orchestrator/Cargo.toml
  </files>
  <action>
    Create the root Cargo.toml with:
    - `[workspace]` with `members = ["crates/*"]` and `resolver = "2"`
    - `[workspace.dependencies]` declaring ALL shared deps with exact versions from research:
      serde = { version = "1.0", features = ["derive"] }
      serde_json = "1.0"
      thiserror = "2.0"
      anyhow = "1.0"
      clap = { version = "4.5", features = ["derive", "env"] }
      toml = "0.8"
      dotenvy = "0.15"
      chrono = { version = "0.4", features = ["serde"] }
      uuid = { version = "1.8", features = ["v4", "serde"] }
      dirs = "6"
      colored = "3"

    Create .gitignore with /target and standard Rust ignores.

    Create crate Cargo.tomls with these dependencies (using workspace = true):
    - ath-types: serde, serde_json, thiserror, chrono, uuid (NO tokio, NO other ath-* crates)
    - ath-config: serde, toml, dotenvy, dirs, thiserror + ath-types as path dep
    - ath-cli: clap, anyhow, colored + ath-types and ath-config as path deps (binary crate with [[bin]] name = "ath")
    - ath-agents: serde, serde_json + ath-types as path dep (stub only for Phase 1)
    - ath-git: ath-types as path dep (stub only)
    - ath-planner: ath-types as path dep (stub only)
    - ath-orchestrator: ath-types as path dep (stub only)

    Dependency direction is strictly unidirectional: ath-types has ZERO ath-* deps. No circular dependencies.

    Each crate's Cargo.toml must set `version = "0.1.0"` and `edition = "2021"`.
  </action>
  <verify>
    <automated>cd C:/Users/master/Documents &amp;&amp; cargo check --workspace 2>&amp;1 | tail -5</automated>
  </verify>
  <done>All 7 crate Cargo.toml files exist with correct workspace dependency references. `cargo check --workspace` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create minimal compilable source stubs for all crates</name>
  <files>
    crates/ath-types/src/lib.rs,
    crates/ath-config/src/lib.rs,
    crates/ath-cli/src/main.rs,
    crates/ath-agents/src/lib.rs,
    crates/ath-git/src/lib.rs,
    crates/ath-planner/src/lib.rs,
    crates/ath-orchestrator/src/lib.rs
  </files>
  <action>
    Create source files for each crate:

    - ath-types/src/lib.rs: Empty module declarations as comments indicating where modules will go (agent, project, review, phase, error). For now, just a doc comment and empty content so it compiles.
    - ath-config/src/lib.rs: Empty pub struct placeholder and doc comment. Must `use ath_types;` (or reference it) so the dependency is validated by the compiler.
    - ath-cli/src/main.rs: Minimal main() that uses clap to parse a --version flag and prints version. Use `clap::Parser` derive. The binary should respond to `ath --version` with the package version. Include a placeholder `run` subcommand. Use anyhow::Result as main return type.
    - ath-agents/src/lib.rs: Doc comment stub only.
    - ath-git/src/lib.rs: Doc comment stub only.
    - ath-planner/src/lib.rs: Doc comment stub only.
    - ath-orchestrator/src/lib.rs: Doc comment stub only.

    The CLI should feel like a real tool from the start -- `ath --version` prints "ath 0.1.0", `ath --help` shows subcommands.
  </action>
  <verify>
    <automated>cd C:/Users/master/Documents &amp;&amp; cargo build --workspace 2>&amp;1 | tail -3 &amp;&amp; cargo run -p ath-cli -- --version</automated>
  </verify>
  <done>`cargo build --workspace` succeeds. `cargo run -p ath-cli -- --version` prints version string. `cargo run -p ath-cli -- --help` shows help with subcommands.</done>
</task>

</tasks>

<verification>
- `cargo check --workspace` passes with zero errors
- `cargo run -p ath-cli -- --version` outputs version
- `cargo run -p ath-cli -- --help` shows usage
- All 7 crate directories exist under crates/
- No circular dependencies (ath-types depends on zero ath-* crates)
</verification>

<success_criteria>
The Cargo workspace compiles cleanly. All 7 crates exist with correct dependency wiring. The `ath` binary responds to --version and --help. The foundation is ready for Plans 02-04 to add real implementations.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
