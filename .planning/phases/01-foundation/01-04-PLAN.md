---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - crates/ath-cli/Cargo.toml
  - crates/ath-cli/src/main.rs
autonomous: true
requirements: [INPT-04, PLAN-04]

must_haves:
  truths:
    - "`ath --version` succeeds and prints version"
    - "Config is loaded on startup -- available providers are reported"
    - "Invalid or missing API key produces a typed error with clear message identifying which key is missing"
    - "Error output uses red 'Error:' prefix with fix suggestion on next line"
  artifacts:
    - path: "crates/ath-cli/src/main.rs"
      provides: "Binary entry point wiring config load and error display"
      min_lines: 40
  key_links:
    - from: "crates/ath-cli/src/main.rs"
      to: "ath_config::ConfigStore"
      via: "ConfigStore::load() call on startup"
      pattern: "ConfigStore::load"
    - from: "crates/ath-cli/src/main.rs"
      to: "ath_types"
      via: "Type imports for schema validation"
      pattern: "use ath_types"
    - from: "crates/ath-cli/src/main.rs"
      to: "colored"
      via: "Error formatting with red prefix"
      pattern: 'use colored::Colorize'
---

<objective>
Wire ath-cli binary to load config on startup, display provider availability, and format errors with the project's error style (red prefix + fix hint). Validates that ath-types and ath-config integrate correctly at the binary boundary.

Purpose: This is the integration point that proves the foundation works end-to-end. Success criteria SC-1 ("ath --version succeeds and config loads") and SC-2 ("invalid API key produces typed error") are validated here. This plan also satisfies the remaining pieces of INPT-04 (config accessible from CLI) and PLAN-04 (schemas importable from binary crate).

Output: Working `ath` binary that loads config, reports provider status, and displays errors correctly.
</objective>

<execution_context>
@C:/Users/master/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CLI entry point with config loading and error display</name>
  <files>
    crates/ath-cli/Cargo.toml,
    crates/ath-cli/src/main.rs
  </files>
  <action>
    Update ath-cli/src/main.rs to be a real CLI entry point:

    1. **Clap struct**: Define `Cli` struct with clap derive:
       - `#[command(name = "ath", version, about = "Multi-agent AI orchestrator")]`
       - Subcommands enum: Run { description: Option<String> }, Init, Report
       - Global flags: --verbose (bool), --no-color (bool)

    2. **Main function**: `fn main() -> anyhow::Result<()>`
       - Parse CLI args
       - Respect --no-color flag and NO_COLOR env var for colored output (call colored::control::set_override if needed)
       - Load config via ConfigStore::load(). On error, format using the error display style below and exit.
       - On success, if --verbose, print which providers are available (e.g., "Providers: Anthropic (configured), Google (not configured), OpenAI (configured)")
       - For subcommands: Run/Init/Report all print "Not yet implemented" placeholder messages for now. This is Phase 1 -- subcommand implementations come in later phases.

    3. **Error display helper**: Create a `fn display_error(err: &anyhow::Error)` function that:
       - Prints "Error: {message}" with "Error:" in red (using colored crate)
       - If the underlying error is a ConfigError, prints the fix hint on the next line: "  Fix: {hint}"
       - If --verbose was passed, prints the full error chain

    4. **Ensure Cargo.toml** has correct dependencies: ath-types and ath-config as path deps, clap, anyhow, colored as workspace deps.

    The binary should feel like a real CLI tool. `ath --version` prints "ath 0.1.0". `ath --help` shows the three subcommands. `ath run` loads config and reports status.
  </action>
  <verify>
    <automated>cd C:/Users/master/Documents &amp;&amp; cargo build -p ath-cli 2>&amp;1 | tail -3 &amp;&amp; cargo run -p ath-cli -- --version &amp;&amp; cargo run -p ath-cli -- --help &amp;&amp; cargo test --workspace 2>&amp;1 | tail -10</automated>
  </verify>
  <done>`ath --version` prints version. `ath --help` shows Run/Init/Report subcommands. `ath run` loads config without panicking. Error display uses red prefix with fix hints. `cargo test --workspace` passes all tests across all crates.</done>
</task>

</tasks>

<verification>
- `cargo run -p ath-cli -- --version` prints "ath 0.1.0"
- `cargo run -p ath-cli -- --help` shows subcommands
- `cargo run -p ath-cli -- run --verbose` loads config and reports provider availability
- `cargo test --workspace` passes all tests (full workspace regression)
- Error messages follow the red "Error:" prefix + fix hint style
</verification>

<success_criteria>
The `ath` binary is a working CLI that loads configuration, reports provider status, and displays errors with actionable fix hints. All Phase 1 success criteria are met: SC-1 (ath --version succeeds with config loaded), SC-2 (invalid/missing API key produces typed error with clear message), SC-3 (schemas round-trip -- proven by ath-types tests), SC-4 (all modules import from shared types crate -- no duplicates).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
