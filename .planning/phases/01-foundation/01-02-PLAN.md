---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - crates/ath-types/src/lib.rs
  - crates/ath-types/src/error.rs
  - crates/ath-types/src/agent.rs
  - crates/ath-types/src/project.rs
  - crates/ath-types/src/review.rs
  - crates/ath-types/src/phase.rs
autonomous: true
requirements: [PLAN-04]

must_haves:
  truths:
    - "All inter-agent schemas (ProjectSpec, AgentRequest, AgentResponse, ReviewVerdict, PhaseRecord) serialize to JSON and deserialize back without data loss"
    - "Validation rejects invalid data with typed errors containing fix hints"
    - "AgentKind enum carries model variant string for type-safe agent identification"
  artifacts:
    - path: "crates/ath-types/src/error.rs"
      provides: "ValidationError enum with fix hints"
      exports: ["ValidationError"]
    - path: "crates/ath-types/src/agent.rs"
      provides: "AgentKind enum, AgentRequest, AgentResponse"
      exports: ["AgentKind", "AgentRequest", "AgentResponse"]
    - path: "crates/ath-types/src/project.rs"
      provides: "ProjectSpec, GoalSpec, SkillTag"
      exports: ["ProjectSpec", "GoalSpec", "SkillTag"]
    - path: "crates/ath-types/src/review.rs"
      provides: "ReviewVerdict, Severity, CodeSuggestion"
      exports: ["ReviewVerdict", "Severity", "CodeSuggestion"]
    - path: "crates/ath-types/src/phase.rs"
      provides: "PhaseRecord, ReviewAttempt, TokenUsage, AgentContribution"
      exports: ["PhaseRecord", "ReviewAttempt", "TokenUsage", "AgentContribution"]
  key_links:
    - from: "crates/ath-types/src/review.rs"
      to: "crates/ath-types/src/agent.rs"
      via: "AgentKind import for reviewer field"
      pattern: "use crate::agent::AgentKind"
    - from: "crates/ath-types/src/phase.rs"
      to: "crates/ath-types/src/review.rs"
      via: "ReviewVerdict import for review attempts"
      pattern: "use crate::review::ReviewVerdict"
    - from: "crates/ath-types/src/project.rs"
      to: "crates/ath-types/src/error.rs"
      via: "ValidationError for validate() return"
      pattern: "use crate::error::ValidationError"
---

<objective>
Implement all typed inter-agent schemas in ath-types: error hierarchy, agent identification, project spec, review verdict, and phase record -- with validation methods and round-trip serialization tests.

Purpose: PLAN-04 requires typed JSON schemas at every agent boundary. These types are the contracts that every downstream crate imports. Getting them right in Phase 1 prevents the #1 multi-agent failure mode (schema mismatches at boundaries).

Output: Complete ath-types crate with all inter-agent schemas, validation logic, and comprehensive round-trip tests.
</objective>

<execution_context>
@C:/Users/master/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement error types and all inter-agent schemas</name>
  <files>
    crates/ath-types/src/lib.rs,
    crates/ath-types/src/error.rs,
    crates/ath-types/src/agent.rs,
    crates/ath-types/src/project.rs,
    crates/ath-types/src/review.rs,
    crates/ath-types/src/phase.rs
  </files>
  <behavior>
    - Test: ProjectSpec with valid data serializes to JSON and deserializes back to identical struct
    - Test: ProjectSpec with empty name fails validation with EmptyField error containing hint
    - Test: ProjectSpec with empty goals list fails validation
    - Test: AgentRequest/AgentResponse round-trip through JSON preserves all fields
    - Test: AgentKind::Claude("opus-4") serializes and deserializes correctly
    - Test: AgentKind.provider_name() returns correct provider string for each variant
    - Test: ReviewVerdict with Severity::Critical and passed=false blocks progress
    - Test: ReviewVerdict with Severity::Warning and passed=false does NOT block progress
    - Test: ReviewVerdict round-trip preserves suggestions list
    - Test: PhaseRecord with timestamps, token usage, contributions, and review attempts round-trips
    - Test: ValidationError.hint() returns the fix hint string
  </behavior>
  <action>
    Implement all types following the RESEARCH.md code examples closely. All types get `#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]`.

    **error.rs:** ValidationError enum with EmptyField { field, hint } and InvalidValue { field, reason, hint } variants. Derive thiserror::Error. Add hint() method.

    **agent.rs:**
    - AgentKind enum: Claude(String), Gemini(String), Codex(String) with provider_name() method
    - AgentRequest: id (Uuid), agent (AgentKind), prompt (String), context (Option<String>), created_at (DateTime<Utc>)
    - AgentResponse: request_id (Uuid), agent (AgentKind), content (String), tokens (TokenUsage from phase.rs -- or define a shared TokenUsage here and re-export from phase.rs). To avoid circular imports, define TokenUsage in a dedicated submodule or in agent.rs and import from phase.rs. Better approach: define TokenUsage in agent.rs (it's about agent resource consumption) and import it in phase.rs.

    Actually, to keep things clean: define TokenUsage in its own section. The research puts it in phase.rs. Follow the research structure -- define TokenUsage in phase.rs, and AgentResponse references token counts as simple u64 fields (input_tokens, output_tokens) rather than the TokenUsage struct. The full TokenUsage with cost estimation belongs in PhaseRecord context.

    Revised AgentResponse: request_id (Uuid), agent (AgentKind), content (String), input_tokens (u64), output_tokens (u64), created_at (DateTime<Utc>)

    **project.rs:** ProjectSpec, GoalSpec, SkillTag exactly as in research. validate() checks: name non-empty, goals non-empty, each goal description non-empty.

    **review.rs:** Severity enum (Critical, Warning, Info) with serde rename_all = "lowercase". CodeSuggestion { file, line: Option<u32>, suggestion }. ReviewVerdict { passed, reviewer: AgentKind, severity, reason, suggestions }. blocks_progress() method. validate(): if severity is Critical and !passed, reason must be non-empty.

    **phase.rs:** TokenUsage { input_tokens: u64, output_tokens: u64, estimated_cost_usd: f64 }. AgentContribution { agent: AgentKind, tokens: TokenUsage, files_produced: Vec<String> }. ReviewAttempt { attempt_number: u32, verdict: ReviewVerdict, timestamp: DateTime<Utc> }. PhaseRecord { id: Uuid, phase_name: String, started_at: DateTime<Utc>, completed_at: Option<DateTime<Utc>>, contributions: Vec<AgentContribution>, review_attempts: Vec<ReviewAttempt> }.

    **lib.rs:** Declare all modules (pub mod error, agent, project, review, phase) and re-export key types at crate root for ergonomic imports.

    NO HashMap-based extensible fields. NO tokio dependency. All fields are concrete types.
    All timestamps use chrono DateTime<Utc> which serializes to RFC 3339 by default.
    UUIDs use uuid::Uuid with v4 generation.

    Write inline #[cfg(test)] mod tests in each module file with the round-trip and validation tests described in the behavior block.
  </action>
  <verify>
    <automated>cd C:/Users/master/Documents &amp;&amp; cargo test -p ath-types -- --nocapture 2>&amp;1 | tail -20</automated>
  </verify>
  <done>All inter-agent schemas compile and have passing round-trip serialization tests. Validation methods reject invalid data with typed errors containing fix hints. `cargo test -p ath-types` passes all tests.</done>
</task>

</tasks>

<verification>
- `cargo test -p ath-types` passes all round-trip and validation tests
- `cargo check --workspace` still passes (no regressions in other crates)
- All 5 schema types (ProjectSpec, AgentRequest, AgentResponse, ReviewVerdict, PhaseRecord) have JSON round-trip tests
- ValidationError includes fix hints
- No HashMap or untyped fields anywhere
</verification>

<success_criteria>
Every inter-agent schema type can be serialized to JSON and deserialized back without data loss. Validation catches invalid data with actionable error messages. The ath-types crate is the single source of truth for all shared types -- no duplicates elsewhere.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
