---
phase: 10-parallel-execution
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - crates/ath-orchestrator/src/coordinator.rs
autonomous: true
requirements:
  - ORCH-04

must_haves:
  truths:
    - "Two independent parallel phases start executing concurrently, not sequentially"
    - "Parallel execution of independent phases completes faster than forced sequential execution"
    - "IsolationManager blocks dispatch when two parallel phases claim the same file"
    - "After parallel phases complete, results contain correct per-phase metadata in deterministic order"
  artifacts:
    - path: "crates/ath-orchestrator/src/coordinator.rs"
      provides: "Integration tests for parallel dispatch"
      contains: "parallel_phases_overlap"
  key_links:
    - from: "crates/ath-orchestrator/src/coordinator.rs (tests)"
      to: "crates/ath-orchestrator/src/coordinator.rs (run_plan_with_progress)"
      via: "Integration tests exercising parallel group paths"
      pattern: "parallel_groups.*vec!.*vec!"
---

<objective>
Add integration tests proving parallel execution works correctly: concurrent dispatch, faster-than-sequential timing, isolation enforcement, and deterministic result ordering.

Purpose: These tests exercise the multi-phase-group path added in plan 01, verifying all four ORCH-04 success criteria from the roadmap.
Output: Integration tests in coordinator.rs test module.
</objective>

<execution_context>
@C:/Users/Letta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Letta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-parallel-execution/10-RESEARCH.md
@.planning/phases/10-parallel-execution/10-01-SUMMARY.md

<interfaces>
<!-- From plan 01 output -- executor should verify these exist before writing tests -->

From crates/ath-orchestrator/src/error.rs (added in plan 01):
```rust
PhaseRunnerError::IsolationViolation { details: String }
PhaseRunnerError::ParallelPhaseFailed { phase_name: String, reason: String }
```

From crates/ath-orchestrator/src/coordinator.rs (modified in plan 01):
```rust
// AgentCoordinator now wraps registry in Arc<AgentRegistry>
// run_plan_with_progress now iterates parallel_groups
// Multi-phase groups use JoinSet + commit gate
```

From crates/ath-agents/src/mock.rs:
```rust
pub struct MockBackend { ... }
impl MockBackend {
    pub fn new(responses: Vec<Result<AgentResponse, AgentError>>) -> Self;
    pub fn always_ok(content: &str) -> Self;
}
// MockBackend uses Mutex<VecDeque> internally -- safe for concurrent access
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parallel dispatch integration tests</name>
  <files>crates/ath-orchestrator/src/coordinator.rs</files>
  <behavior>
    - Test: parallel_phases_overlap -- Two phases in the same parallel group execute concurrently (both start before either completes). Use a DelayedMockBackend or tokio::time::sleep inside a mock to prove overlap. Assert both PhaseRecords have `completed_at` set and the total wall-clock time is less than the sum of individual phase times.
    - Test: parallel_faster_than_sequential -- Create a plan with two independent phases using `parallel_groups: vec![vec![1, 2]]` and the same plan with `parallel_groups: vec![vec![1], vec![2]]` (forced sequential). Add a small delay (50ms) to mock responses. Assert parallel wall-clock time is less than sequential wall-clock time.
    - Test: parallel_isolation_blocks_conflict -- Create a plan where two phases in the same parallel group claim the same output file. Assert `run_plan` returns `Err(PhaseRunnerError::IsolationViolation { .. })` and no LLM calls were made (mock call count is 0).
    - Test: parallel_commits_correct_metadata -- Create a plan with two parallel phases producing different files. Assert both PhaseRecords appear in results, sorted by phase_id, with correct `phase_name` and `completed_at` set. Verify both sets of files exist in `output_dir`.
  </behavior>
  <action>
Add these four integration tests to the existing `#[cfg(test)] mod tests` block in `coordinator.rs`. Reuse existing test helpers (`make_task_spec`, `make_phase`, `mock_response`, `passing_verdict_json`, `make_task_output_json`).

**For timing tests:** Use `MockBackend` with a wrapper that adds `tokio::time::sleep(Duration::from_millis(50))` before returning. The simplest approach: create a `DelayedMockBackend` struct in the test module that wraps `MockBackend` and implements `AgentBackend` by delegating to the inner mock after a sleep. Alternatively, if `MockBackend` already supports async delays through its trait impl, use `tokio::time::Instant` to measure wall-clock time.

**For the overlap test:** Use `Arc<AtomicU32>` as a concurrency counter. Each spawned phase increments on start (before sleep), then decrements on complete (after sleep). Assert the counter reached 2 at some point during execution (proving overlap). This can be done via a `DelayedMockBackend` that records `max_concurrent` into a shared `Arc<AtomicU32>`.

**For the isolation test:** Create a `make_plan_with_groups` helper or modify `make_plan` to accept `parallel_groups` directly (it may already do so from plan 01 updates). Set two phases in the same group with overlapping `expected_output_files`. Assert the error is `IsolationViolation`.

**For the metadata test:** Use `always_ok` mocks with two phases in `parallel_groups: vec![vec![1, 2]]`, each producing different files. Assert `records[0].phase_name` and `records[1].phase_name` are in phase_id order regardless of completion order. Assert files from both phases exist in the temp output dir.

**Important:** Use `#[tokio::test]` for all tests. Use generous timing thresholds (e.g., parallel time < 150ms vs sequential time > 80ms) to avoid flakiness.
  </action>
  <verify>
    <automated>cargo test -p ath-orchestrator parallel_ -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>
- `parallel_phases_overlap` proves concurrent execution
- `parallel_faster_than_sequential` proves wall-clock improvement
- `parallel_isolation_blocks_conflict` proves pre-dispatch isolation
- `parallel_commits_correct_metadata` proves deterministic results and file output
- All four tests pass, all existing tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Workspace-wide regression check and cleanup</name>
  <files>crates/ath-orchestrator/src/coordinator.rs</files>
  <action>
Run the full workspace test suite and clippy to catch any regressions from the parallel execution changes.

1. Run `cargo test --workspace` and fix any failures.
2. Run `cargo clippy --workspace -- -D warnings` and fix any warnings.
3. If any progress-related tests fail due to the new parallel dispatch path (e.g., progress event ordering assumptions), update those tests to handle both sequential and parallel event orderings.
4. Verify the CLI integration still compiles: `cargo build -p ath-cli`.

No new files should be created. This is a verification and cleanup task.
  </action>
  <verify>
    <automated>cargo test --workspace 2>&1 | tail -5 && cargo clippy --workspace -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>
- `cargo test --workspace` passes with 0 failures
- `cargo clippy --workspace -- -D warnings` passes with 0 warnings
- `cargo build -p ath-cli` succeeds
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p ath-orchestrator parallel_` -- all 4 parallel tests pass
2. `cargo test --workspace` -- full suite green
3. `cargo clippy --workspace -- -D warnings` -- clean
</verification>

<success_criteria>
- Four integration tests prove all ORCH-04 success criteria from the roadmap
- No regressions in existing tests across the workspace
- No clippy warnings
</success_criteria>

<output>
After completion, create `.planning/phases/10-parallel-execution/10-02-SUMMARY.md`
</output>
