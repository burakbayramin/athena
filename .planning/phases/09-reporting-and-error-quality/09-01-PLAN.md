---
phase: 09-reporting-and-error-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ath-types/src/lib.rs
  - crates/ath-types/src/phase.rs
  - crates/ath-types/src/report.rs
  - crates/ath-orchestrator/src/phase_runner.rs
  - crates/ath-cli/src/report.rs
  - crates/ath-cli/src/run.rs
autonomous: true
requirements: [OUTP-03]

must_haves:
  truths:
    - "A successful `ath run` persists a structured run report under a project-local `.ath/runs/` path"
    - "The persisted report joins routed plan metadata with completed phase execution data through a stable phase identifier"
    - "`ath report` can resolve the latest saved run without requiring an explicit target"
    - "Saved report artifacts are structured enough to render phase/task/agent/dependency/review summaries without re-running Athena"
  artifacts:
    - path: "crates/ath-types/src/report.rs"
      provides: "Serializable run-report schema combining routed plan metadata and execution results"
      contains: "RunReport"
    - path: "crates/ath-cli/src/report.rs"
      provides: "Filesystem helpers for saving/loading report artifacts and latest-run resolution"
      contains: "load_report"
    - path: "crates/ath-cli/src/run.rs"
      provides: "Post-run report persistence hook on the successful execution path"
      contains: "write_run_report"
  key_links:
    - from: "crates/ath-cli/src/run.rs"
      to: "crates/ath-cli/src/report.rs"
      via: "successful runs write a saved report artifact before returning to the terminal"
      pattern: "write_run_report"
    - from: "crates/ath-cli/src/report.rs"
      to: "crates/ath-types/src/report.rs"
      via: "CLI persistence and loading operate on a typed RunReport contract"
      pattern: "RunReport"
    - from: "crates/ath-orchestrator/src/phase_runner.rs"
      to: "crates/ath-types/src/phase.rs"
      via: "phase records carry a stable phase identifier that report building can join back to the routed plan"
      pattern: "phase_id"
---

<objective>
Introduce Athena's durable run-report artifact so every successful run leaves behind structured output that `ath report` can load later.

Purpose: Phase 9 cannot satisfy `OUTP-03` until `ath run` writes a saved report artifact that preserves both the routed plan metadata and the completed execution/review outcomes.

Output: typed run-report schemas, latest-run lookup helpers, and successful-run persistence wiring in `ath-cli`.
</objective>

<execution_context>
@C:/Users/Letta/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Letta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-reporting-and-error-quality/09-RESEARCH.md
@.planning/phases/09-reporting-and-error-quality/09-VALIDATION.md

<interfaces>
From crates/ath-cli/src/run.rs:
```rust
let records = coordinator
    .run_plan_with_progress(&plan, Some(observer))
    .await
    .map_err(|e| anyhow!("{e}"))?;
```

From crates/ath-types/src/phase.rs:
```rust
pub struct PhaseRecord {
    pub id: Uuid,
    pub phase_name: String,
    pub started_at: DateTime<Utc>,
    pub completed_at: Option<DateTime<Utc>>,
    pub contributions: Vec<AgentContribution>,
    pub review_attempts: Vec<ReviewAttempt>,
}
```

From Phase 9 research:
- `PhaseRecord` alone is not enough for the final report; the persisted artifact must also retain routed `ExecutionPlan` metadata
- `ath report` needs a project-local latest-run pointer instead of reusing live execution state
- `.ath/last-plan.json` already establishes project-local artifact storage as a valid pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed run-report schemas and stable phase joins for report persistence</name>
  <files>crates/ath-types/src/lib.rs, crates/ath-types/src/phase.rs, crates/ath-types/src/report.rs, crates/ath-orchestrator/src/phase_runner.rs</files>
  <behavior>
    - Test 1: `RunReport` (or equivalent) serializes and deserializes with routed plan metadata plus completed phase records
    - Test 2: `PhaseRecord` exposes a stable plan-join field (`phase_id`) rather than relying on phase-name matching
    - Test 3: Existing phase-record round-trip tests still pass with the new report-facing schema
  </behavior>
  <action>
    1. Create `crates/ath-types/src/report.rs` with the serializable report artifact Athena will persist after runs. Include routed execution-plan metadata, completed phase records, and room for aggregate totals.
    2. Export the new report types from `crates/ath-types/src/lib.rs`.
    3. Extend `PhaseRecord` with a stable phase identifier that report building can join back to `ExecutionPlan` entries without depending on human-readable names.
    4. Update `run_phase_with_progress()` to populate that stable phase identifier on every successful phase record.
  </action>
  <verify>
    <automated>cargo test -p ath-types report</automated>
  </verify>
  <done>The codebase has a typed, serializable report artifact and `PhaseRecord` is safe to join back to routed plan metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist run reports under `.ath/runs` and wire latest-run resolution into `ath report`</name>
  <files>crates/ath-cli/src/report.rs, crates/ath-cli/src/run.rs</files>
  <behavior>
    - Test 1: A successful synthetic run writes a run directory with a structured report artifact
    - Test 2: The latest-run pointer resolves the most recent report when `ath report` receives no explicit target
    - Test 3: `ath report <target>` can load an explicit saved report artifact without re-executing anything
    - Test 4: Saved artifacts contain routed plan rows plus review outcomes, not just a completion banner
  </behavior>
  <action>
    1. Add project-local report persistence helpers in `crates/ath-cli/src/report.rs` for a `.ath/runs/<run-id>/` layout plus a latest-run pointer.
    2. Build the saved report from the routed `ExecutionPlan` and returned `Vec<PhaseRecord>` on the success path in `run::run_command`.
    3. Update `ath report` so its default target resolves through the latest-run pointer and its explicit target path loads a saved artifact instead of printing a placeholder.
    4. Keep the saved artifact format deterministic and test-friendly; JSON is mandatory, and markdown/human-readable output may be generated alongside it if convenient.
  </action>
  <verify>
    <automated>cargo test -p ath-cli report::tests</automated>
  </verify>
  <done>Every successful Athena run leaves behind a loadable run artifact, and `ath report` has a real latest-run contract instead of a placeholder.</done>
</task>

</tasks>

<verification>
- `cargo test -p ath-types report`
- `cargo test -p ath-cli report::tests`
- `cargo test -p ath-cli`
- `cargo test --workspace`
</verification>

<success_criteria>
- Successful runs write a structured report artifact under `.ath/runs`
- Report artifacts preserve routed phase metadata and review outcomes together
- `ath report` can resolve the latest saved run by default
- Report loading does not require re-planning or re-running Athena
</success_criteria>

<output>
After completion, create `.planning/phases/09-reporting-and-error-quality/09-01-SUMMARY.md`
</output>
