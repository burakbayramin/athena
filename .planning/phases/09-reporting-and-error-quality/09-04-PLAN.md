---
phase: 09-reporting-and-error-quality
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - crates/ath-cli/src/main.rs
  - crates/ath-agents/src/error.rs
  - crates/ath-orchestrator/src/error.rs
  - crates/ath-orchestrator/src/phase_runner.rs
  - crates/ath-types/src/error.rs
autonomous: true
requirements: [OUTP-04]

must_haves:
  truths:
    - "Provider authentication failures name the provider and point to the env var or config key the user should check"
    - "Review failure errors include phase name, reviewer identity, failed attempt number, and the verdict text that caused the halt"
    - "Schema/validation errors identify the field and the offending raw value or received content when available"
    - "CLI error rendering stays centralized instead of scattering custom formatting across subcommands"
  artifacts:
    - path: "crates/ath-cli/src/main.rs"
      provides: "Centralized actionable CLI error rendering for provider, review, and schema failures"
      contains: "display_error"
    - path: "crates/ath-agents/src/error.rs"
      provides: "Provider error hints with actionable auth/config guidance"
      contains: "AuthFailed"
    - path: "crates/ath-orchestrator/src/error.rs"
      provides: "Review failure errors carrying reviewer and attempt context"
      contains: "MaxRetriesExceeded"
    - path: "crates/ath-types/src/error.rs"
      provides: "Schema/validation errors that can surface offending raw values"
      contains: "InvalidValue"
  key_links:
    - from: "crates/ath-cli/src/main.rs"
      to: "crates/ath-agents/src/error.rs"
      via: "CLI downcasts provider failures and prints actionable auth/config guidance"
      pattern: "AgentError"
    - from: "crates/ath-cli/src/main.rs"
      to: "crates/ath-orchestrator/src/error.rs"
      via: "CLI downcasts review/execution failures and prints reviewer/attempt context"
      pattern: "PhaseRunnerError"
    - from: "crates/ath-cli/src/main.rs"
      to: "crates/ath-types/src/error.rs"
      via: "CLI downcasts schema/validation failures and prints field-level fix guidance"
      pattern: "ValidationError"
---

<objective>
Upgrade Athena's terminal error output so provider, review, and schema failures tell users what failed and what to do next without reading source code.

Purpose: `OUTP-04` is only satisfied if the CLI preserves the rich context already present in typed errors and fills in the missing reviewer/raw-value details where the current error variants are still too weak.

Output: richer typed error data where necessary, plus centralized actionable error rendering in `ath-cli`.
</objective>

<execution_context>
@C:/Users/Letta/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Letta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-reporting-and-error-quality/09-RESEARCH.md
@.planning/phases/09-reporting-and-error-quality/09-VALIDATION.md
@.planning/phases/09-reporting-and-error-quality/09-03-PLAN.md

<interfaces>
From crates/ath-cli/src/main.rs:
```rust
fn display_error(err: &anyhow::Error) {
    eprintln!("{} {}", "Error:".red(), err);
    if let Some(config_err) = err.downcast_ref::<ConfigError>() {
        eprintln!("  {} {}", "Fix:".yellow(), config_err.hint());
    }
}
```

From crates/ath-agents/src/error.rs:
```rust
pub enum AgentError {
    RateLimit { provider: String, retry_after: Option<Duration> },
    AuthFailed { provider: String, reason: String },
    ServerError { provider: String, status: u16 },
    ...
}
```

From crates/ath-orchestrator/src/error.rs:
```rust
pub enum PhaseRunnerError {
    MaxRetriesExceeded { phase_name: String, attempts: u32, final_reason: String },
    ...
}
```

From Phase 9 research:
- CLI currently only renders extra guidance for `ConfigError`
- review failure messages still lack reviewer identity and verdict text
- validation errors already have hints, but the raw offending value is not always surfaced
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich typed errors with the context users actually need</name>
  <files>crates/ath-agents/src/error.rs, crates/ath-orchestrator/src/error.rs, crates/ath-orchestrator/src/phase_runner.rs, crates/ath-types/src/error.rs</files>
  <behavior>
    - Test 1: Provider auth failures expose an actionable hint naming the env var or config key to check
    - Test 2: Max-retry review failures preserve reviewer identity, failed attempt number, and verdict reason text
    - Test 3: Validation/schema failures can include the offending field and raw received value when available
  </behavior>
  <action>
    1. Add hint/accessor helpers or richer fields to `AgentError` so auth and rate-limit failures can surface provider-specific remediation.
    2. Expand `PhaseRunnerError::MaxRetriesExceeded` (or the equivalent halt error) so the CLI can name the reviewer and failed attempt, not just the phase and final reason.
    3. Extend `ValidationError` or the schema-deserialization path with the minimum extra context needed to show the bad field/value to users.
    4. Update the construction sites in `phase_runner.rs` and any validation callers so the richer error data is actually populated.
  </action>
  <verify>
    <automated>cargo test -p ath-agents error::tests && cargo test -p ath-orchestrator error::tests && cargo test -p ath-types error::tests</automated>
  </verify>
  <done>The typed error layer contains enough structured context for the CLI to render actionable provider, review, and schema failure messages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Centralize actionable provider/review/schema error rendering in the CLI</name>
  <files>crates/ath-cli/src/main.rs</files>
  <behavior>
    - Test 1: Provider auth errors show the provider plus a concrete configuration fix
    - Test 2: Review failure errors show phase, reviewer, attempt number, and verdict text
    - Test 3: Validation/schema errors show the field and fix guidance without dumping unreadable debug output
    - Test 4: Existing `ConfigError` hint behavior still works unchanged
  </behavior>
  <action>
    1. Extend `display_error()` so it downcasts and renders `AgentError`, `PhaseRunnerError`, and `ValidationError` in addition to `ConfigError`.
    2. Keep formatting centralized in `main.rs`; command modules should keep returning typed errors rather than inventing their own output rules.
    3. Add focused CLI-facing tests that assert the rendered terminal text contains the required provider/reviewer/field guidance.
  </action>
  <verify>
    <automated>cargo test -p ath-cli error_display::tests</automated>
  </verify>
  <done>Athena's terminal errors now distinguish provider, review, and schema failures with actionable next-step guidance.</done>
</task>

</tasks>

<verification>
- `cargo test -p ath-agents error::tests`
- `cargo test -p ath-orchestrator error::tests`
- `cargo test -p ath-types error::tests`
- `cargo test -p ath-cli error_display::tests`
- `cargo test --workspace`
</verification>

<success_criteria>
- Provider auth/config failures identify the provider and remediation path
- Review failure halts show reviewer identity, attempt number, and verdict text
- Schema/validation failures identify the field and bad value when available
- CLI error formatting remains centralized and testable
</success_criteria>

<output>
After completion, create `.planning/phases/09-reporting-and-error-quality/09-04-SUMMARY.md`
</output>
