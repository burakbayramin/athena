---
phase: 09-reporting-and-error-quality
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - crates/ath-cli/src/report.rs
  - crates/ath-cli/src/run.rs
  - crates/ath-types/src/report.rs
  - crates/ath-cli/src/cost.rs
autonomous: true
requirements: [QUAL-04, OUTP-03]

must_haves:
  truths:
    - "Known provider/model combinations produce non-zero estimated costs from token counts"
    - "Per-agent, per-phase, and whole-run totals are computed from the saved run-report artifact"
    - "Unknown pricing entries fail clearly or render as explicit `n/a` values instead of silently pretending to know the cost"
    - "Human-readable reports surface token and cost totals alongside review outcomes and phase status"
  artifacts:
    - path: "crates/ath-cli/src/cost.rs"
      provides: "Provider/model pricing lookup and cost calculation helpers"
      contains: "estimate_cost"
    - path: "crates/ath-cli/src/report.rs"
      provides: "Report aggregation and human-readable rendering with token/cost totals"
      contains: "render_report"
    - path: "crates/ath-types/src/report.rs"
      provides: "Run-report totals schema persisted to disk"
      contains: "ReportTotals"
  key_links:
    - from: "crates/ath-cli/src/report.rs"
      to: "crates/ath-cli/src/cost.rs"
      via: "saved run reports are enriched with estimated costs through a pricing helper"
      pattern: "estimate_cost"
    - from: "crates/ath-cli/src/report.rs"
      to: "crates/ath-types/src/report.rs"
      via: "aggregated token/cost totals are persisted in the structured run report"
      pattern: "ReportTotals"
---

<objective>
Add real token-cost estimation so Athena's saved reports and `ath report` output can show meaningful usage totals per agent, per phase, and for the whole run.

Purpose: `QUAL-04` requires more than raw token counts. The saved report must turn those counts into estimated dollar cost using known provider/model pricing, while failing honestly when pricing is unavailable.

Output: pricing helpers, report aggregation totals, and human-readable cost sections on top of the persisted run-report artifact.
</objective>

<execution_context>
@C:/Users/Letta/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Letta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-reporting-and-error-quality/09-RESEARCH.md
@.planning/phases/09-reporting-and-error-quality/09-VALIDATION.md
@.planning/phases/09-reporting-and-error-quality/09-01-PLAN.md
@.planning/phases/09-reporting-and-error-quality/09-02-PLAN.md

<interfaces>
From crates/ath-types/src/phase.rs:
```rust
pub struct TokenUsage {
    pub input_tokens: u64,
    pub output_tokens: u64,
    pub estimated_cost_usd: f64,
}
```

From crates/ath-types/src/agent.rs:
```rust
impl AgentKind {
    pub fn provider_name(&self) -> &str;
    pub fn model(&self) -> &str;
}
```

From Phase 9 research:
- cost estimation should be isolated from report aggregation
- report rendering should consume structured data first, then produce terminal-friendly summaries
- unknown model/provider pricing must not silently become fake numbers
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add provider/model pricing helpers and estimate contribution costs honestly</name>
  <files>crates/ath-cli/src/cost.rs, crates/ath-cli/src/report.rs, crates/ath-types/src/report.rs</files>
  <behavior>
    - Test 1: Known provider/model pairs produce deterministic estimated costs from token counts
    - Test 2: Unknown provider/model pairs return an explicit unsupported-pricing result instead of pretending the cost is zero
    - Test 3: Aggregated phase and run totals sum contribution costs correctly
  </behavior>
  <action>
    1. Create a dedicated pricing helper module keyed by `AgentKind` provider/model data.
    2. Use that helper when building or enriching persisted run reports so contribution, phase, and run totals all carry estimated cost fields.
    3. Represent unsupported pricing explicitly in the report layer (`n/a`, omitted estimate, or typed unsupported-pricing state), but do not silently fabricate a zero-dollar estimate for unknown models.
  </action>
  <verify>
    <automated>cargo test -p ath-cli report::tests::computes_per_phase_and_run_costs</automated>
  </verify>
  <done>Saved reports and terminal summaries can show honest cost estimates with explicit behavior for unknown pricing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface token and cost totals in the human-readable report output</name>
  <files>crates/ath-cli/src/report.rs, crates/ath-cli/src/run.rs, crates/ath-types/src/report.rs</files>
  <behavior>
    - Test 1: `ath report` shows per-phase token and cost totals in the human-readable summary
    - Test 2: The overall run summary includes total input tokens, output tokens, and estimated cost
    - Test 3: Review outcomes remain visible alongside the new usage/cost sections
  </behavior>
  <action>
    1. Extend the run-report artifact with aggregate totals structured for both machine-readable persistence and terminal rendering.
    2. Update `ath report` rendering so the default human-readable summary includes phase-by-phase usage/cost sections plus overall totals.
    3. Ensure the saved artifact and the terminal summary stay derived from the same structured report data rather than maintaining two separate aggregation paths.
  </action>
  <verify>
    <automated>cargo test -p ath-cli report::tests</automated>
  </verify>
  <done>`ath report` now presents token and estimated-cost totals clearly, backed by the same saved report data written after execution.</done>
</task>

</tasks>

<verification>
- `cargo test -p ath-cli report::tests::computes_per_phase_and_run_costs`
- `cargo test -p ath-cli report::tests`
- `cargo test -p ath-cli`
- `cargo test --workspace`
</verification>

<success_criteria>
- Cost estimation works for known priced models
- Unknown pricing does not silently become fake zero-cost output
- Reports show per-agent, per-phase, and total token/cost figures
- Human-readable and persisted totals come from the same structured report data
</success_criteria>

<output>
After completion, create `.planning/phases/09-reporting-and-error-quality/09-03-SUMMARY.md`
</output>
