---
phase: 09-reporting-and-error-quality
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - crates/ath-orchestrator/src/phase_runner.rs
  - crates/ath-types/src/phase.rs
autonomous: true
requirements: [QUAL-04, OUTP-03]

must_haves:
  truths:
    - "Phase records include token usage from both executor work and reviewer passes/failures"
    - "Retry attempts accumulate token usage across the full phase instead of only keeping the final attempt"
    - "Per-agent accounting keeps model distinctions intact rather than collapsing all agents by provider discriminant"
    - "Persisted report data can derive per-phase and per-agent token totals without consulting transcripts"
  artifacts:
    - path: "crates/ath-orchestrator/src/phase_runner.rs"
      provides: "Complete token accounting across executor tasks, reviewers, and retries"
      contains: "review_response"
    - path: "crates/ath-types/src/phase.rs"
      provides: "Phase record schema that can carry report-grade token usage totals"
      contains: "TokenUsage"
  key_links:
    - from: "crates/ath-orchestrator/src/phase_runner.rs"
      to: "crates/ath-types/src/phase.rs"
      via: "phase records and agent contributions expose the token totals later consumed by the report writer"
      pattern: "AgentContribution"
    - from: "crates/ath-orchestrator/src/phase_runner.rs"
      to: "crates/ath-cli/src/report.rs"
      via: "saved run reports consume the completed contribution totals built during phase execution"
      pattern: "PhaseRecord"
---

<objective>
Finish Athena's token accounting so Phase 9 can report accurate per-phase and per-agent usage totals instead of partial executor-only counts.

Purpose: `QUAL-04` is not satisfied by the current code because reviewer token usage is omitted and phase contributions are aggregated too loosely for report-quality totals.

Output: review-inclusive token accounting in `PhaseRecord`, retry-safe accumulation, and tests proving the saved report input data is complete.
</objective>

<execution_context>
@C:/Users/Letta/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Letta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-reporting-and-error-quality/09-RESEARCH.md
@.planning/phases/09-reporting-and-error-quality/09-VALIDATION.md
@.planning/phases/09-reporting-and-error-quality/09-01-PLAN.md

<interfaces>
From crates/ath-orchestrator/src/phase_runner.rs:
```rust
if let Some(contrib) = existing {
    contrib.tokens.input_tokens += output.input_tokens;
    contrib.tokens.output_tokens += output.output_tokens;
} else {
    all_contributions.push(AgentContribution {
        agent: output.agent.clone(),
        tokens: TokenUsage {
            input_tokens: output.input_tokens,
            output_tokens: output.output_tokens,
            estimated_cost_usd: 0.0,
        },
        files_produced: ...
    });
}
```

From Phase 9 research:
- reviewer token usage is currently dropped on the floor
- retries should accumulate across the full phase
- `mem::discriminant`-based aggregation is too coarse for report-grade per-agent accounting
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Record reviewer token usage alongside executor contributions</name>
  <files>crates/ath-orchestrator/src/phase_runner.rs, crates/ath-types/src/phase.rs</files>
  <behavior>
    - Test 1: A passing review adds reviewer token usage into the phase record
    - Test 2: A retrying phase accumulates reviewer token usage across failed and passing review attempts
    - Test 3: Report-facing phase records still serialize and deserialize cleanly with the richer usage data
  </behavior>
  <action>
    1. Update `run_phase_with_progress()` so reviewer `AgentResponse` token counts are captured in `PhaseRecord` data instead of being ignored.
    2. If the existing `PhaseRecord`/`ReviewAttempt` schema is too weak to express that cleanly, extend it with the smallest report-facing field(s) needed and preserve serde round-trips.
    3. Keep retry semantics additive: retries should increase the same phase totals rather than overwrite earlier failed attempts.
  </action>
  <verify>
    <automated>cargo test -p ath-orchestrator phase_runner::tests::run_phase_record_tracks_token_usage</automated>
  </verify>
  <done>Phase records now include reviewer usage, so saved reports can account for the full cost of execution plus review.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make per-agent accumulation exact and report-safe across retries</name>
  <files>crates/ath-orchestrator/src/phase_runner.rs</files>
  <behavior>
    - Test 1: Contributions accumulate by full `AgentKind`, not only by provider discriminant
    - Test 2: Multiple retries for the same agent roll up into one stable contribution record
    - Test 3: File lists remain deduplicated while token totals continue to grow across attempts
  </behavior>
  <action>
    1. Replace the current `mem::discriminant`-based aggregation with a full-agent match that preserves model identity.
    2. Keep contribution merging deterministic across repeated executor and reviewer participation.
    3. Add focused tests that prove retries and repeated review attempts produce complete token totals without duplicate file-path spam.
  </action>
  <verify>
    <automated>cargo test -p ath-orchestrator phase_runner::tests</automated>
  </verify>
  <done>Saved report input data is accurate per agent, per phase, and across the full retry loop.</done>
</task>

</tasks>

<verification>
- `cargo test -p ath-orchestrator phase_runner::tests::run_phase_record_tracks_token_usage`
- `cargo test -p ath-orchestrator phase_runner::tests`
- `cargo test -p ath-types phase::tests`
- `cargo test --workspace`
</verification>

<success_criteria>
- Reviewer token usage is present in phase records
- Retry attempts accumulate usage across the full phase
- Per-agent totals preserve full `AgentKind` identity
- Persisted report data can derive accurate usage totals without reading transcripts
</success_criteria>

<output>
After completion, create `.planning/phases/09-reporting-and-error-quality/09-02-SUMMARY.md`
</output>
