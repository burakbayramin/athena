---
phase: 02-agent-clients
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - crates/ath-agents/tests/integration.rs
  - crates/ath-agents/src/actor/mod.rs
autonomous: false
requirements: [ORCH-05]

must_haves:
  truths:
    - "MockBackend can simulate a full agent request/response cycle through the AgentBackend trait"
    - "Retry behavior is exercised: a transient failure followed by success returns the successful response"
    - "Circuit breaker behavior is exercised: 3 consecutive failures cause subsequent requests to fail with CircuitOpen"
    - "All three provider handles (Claude, Gemini, Codex) have identical behavior through the trait interface"
  artifacts:
    - path: "crates/ath-agents/tests/integration.rs"
      provides: "Integration tests verifying AgentBackend trait behavior across all providers"
      contains: "async fn"
  key_links:
    - from: "crates/ath-agents/tests/integration.rs"
      to: "crates/ath-agents/src/backend.rs"
      via: "Tests exercise AgentBackend trait methods"
      pattern: "AgentBackend"
    - from: "crates/ath-agents/tests/integration.rs"
      to: "crates/ath-agents/src/mock.rs"
      via: "Tests use MockBackend for deterministic behavior"
      pattern: "MockBackend"
---

<objective>
Write integration tests proving the full agent layer works end-to-end: trait-based dispatch, retry with backoff, circuit breaker tripping, and provider handle lifecycle. Then verify the complete phase with a human checkpoint.

Purpose: These tests prove ORCH-05 is satisfied without requiring real API keys. They exercise the full stack (handle -> actor -> retry -> circuit breaker -> mock provider) through the AgentBackend trait interface.

Output: Integration test file and human verification of the complete agent layer.
</objective>

<execution_context>
@C:/Users/master/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-clients/02-CONTEXT.md
@.planning/phases/02-agent-clients/02-RESEARCH.md
@.planning/phases/02-agent-clients/02-01-SUMMARY.md
@.planning/phases/02-agent-clients/02-02-SUMMARY.md

<interfaces>
<!-- Full public API from Plans 01 and 02 -->

From crates/ath-agents/src/lib.rs:
```rust
// Re-exports
pub use error::AgentError;
pub use circuit_breaker::CircuitBreaker;
pub use backend::AgentBackend;
pub use mock::MockBackend;
pub use actor::claude::ClaudeHandle;
pub use actor::gemini::GeminiHandle;
pub use actor::codex::CodexHandle;
```

From crates/ath-types/src/agent.rs:
```rust
pub enum AgentKind { Claude(String), Gemini(String), Codex(String) }
pub struct AgentRequest { pub id: Uuid, pub agent: AgentKind, pub prompt: String, pub context: Option<String>, pub created_at: DateTime<Utc> }
pub struct AgentResponse { pub request_id: Uuid, pub agent: AgentKind, pub content: String, pub input_tokens: u64, pub output_tokens: u64, pub created_at: DateTime<Utc> }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration tests for agent layer</name>
  <files>crates/ath-agents/tests/integration.rs</files>
  <behavior>
    - Test: MockBackend always_ok mode returns successful AgentResponse with correct content
    - Test: MockBackend sequenced mode returns responses in order
    - Test: MockBackend failing mode returns the expected error
    - Test: AgentBackend trait can be used as dyn dispatch (Box&lt;dyn AgentBackend&gt;)
    - Test: CircuitBreaker trips after 3 failures and returns CircuitOpen
    - Test: CircuitBreaker recovers after cooldown (half-open probe succeeds)
    - Test: ClaudeHandle::new fails with AuthFailed when no API key configured
    - Test: GeminiHandle::new fails with AuthFailed when no API key configured
    - Test: CodexHandle::new fails with AuthFailed when no API key configured
  </behavior>
  <action>
Create `crates/ath-agents/tests/integration.rs` as an integration test file (Cargo convention: files in `tests/` directory).

**Test groups:**

1. **MockBackend through AgentBackend trait:**
   - Create a `MockBackend::always_ok("hello world")`.
   - Call `backend.send(request).await` and assert content is "hello world".
   - Verify `backend.is_available().await` returns true.
   - Verify `backend.provider_name()` returns expected string.

2. **MockBackend sequenced responses:**
   - Create `MockBackend::new(vec![Ok(response1), Err(error), Ok(response2)])`.
   - Call send three times, verify responses match in order.

3. **Dynamic dispatch via Box&lt;dyn AgentBackend&gt;:**
   - Create `let backend: Box<dyn AgentBackend> = Box::new(MockBackend::always_ok("test"));`
   - Call `backend.send()` and verify it works through the trait object.
   - This proves Phase 6-7 can use `dyn AgentBackend` for runtime dispatch.

4. **Provider handle construction with missing keys:**
   - Create a `ConfigStore` with all API keys as None (use `ConfigStore::load_from_layers(None, None, empty_config)`).
   - Call `ClaudeHandle::new(&config)` and assert it returns `Err(AgentError::AuthFailed { .. })`.
   - Same for `GeminiHandle::new(&config)` and `CodexHandle::new(&config)`.

5. **Circuit breaker integration** (if actor internals allow testing without real HTTP):
   - This may need to use the shared `run_with_retry_and_breaker()` function directly with a mock call function, or test via the CircuitBreaker unit tests from Plan 01.
   - If direct testing is not feasible without real HTTP, add a comment explaining that circuit breaker behavior is verified by unit tests in `circuit_breaker.rs` and will be validated with real providers during manual testing.

Helper function: `fn make_request(agent: AgentKind) -> AgentRequest` that creates a valid AgentRequest with UUID, prompt, and timestamp.

All tests use `#[tokio::test]`. Import from `ath_agents` crate (external crate perspective).
  </action>
  <verify>
    <automated>cd C:/Users/master/Documents && cargo test -p ath-agents --test integration</automated>
  </verify>
  <done>All integration tests pass. MockBackend works through AgentBackend trait in all modes. Dynamic dispatch via Box&lt;dyn AgentBackend&gt; compiles and runs. Provider handles reject missing API keys at construction.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete agent layer</name>
  <files>crates/ath-agents/</files>
  <action>
Present the complete agent client layer for human review. All code has been automated -- this checkpoint only verifies the results.

What was built across Plans 01-03:
- AgentBackend trait (uniform async interface for all providers)
- AgentError enum (normalized error classification with is_retryable())
- CircuitBreaker state machine (Closed/Open/HalfOpen with configurable threshold and cooldown)
- MockBackend test double (sequenced, always_ok, failing modes)
- ClaudeHandle, GeminiHandle, CodexHandle (tokio actor pattern with mpsc+oneshot, retry via backon, circuit breaker per actor)
- Integration tests proving trait dispatch, dyn dispatch, and provider construction

Verification steps for the human:
1. Run `cd C:/Users/master/Documents && cargo test -p ath-agents` -- all tests should pass
2. Run `cargo test --workspace` -- no regressions
3. Review the public API: `cargo doc -p ath-agents --no-deps --open` -- check that AgentBackend, AgentError, CircuitBreaker, MockBackend, ClaudeHandle, GeminiHandle, CodexHandle are all documented
4. Optional: If you have API keys configured, test a real provider call
  </action>
  <verify>Human confirms all tests pass and public API is correct</verify>
  <done>Human has approved the complete agent client layer</done>
</task>

</tasks>

<verification>
- `cargo test -p ath-agents` passes all unit and integration tests
- `cargo test --workspace` passes with zero regressions
- `cargo doc -p ath-agents --no-deps` generates clean documentation
- Phase success criteria from ROADMAP validated:
  1. Athena can send a prompt to Claude via ClaudeHandle (trait interface proven with MockBackend)
  2. Athena can send a prompt to Gemini via GeminiHandle (trait interface proven with MockBackend)
  3. Athena can send a prompt to Codex via CodexHandle (trait interface proven with MockBackend)
  4. 429/500 triggers exponential backoff via backon (unit tested in circuit_breaker + retry logic)
  5. 3 consecutive failures trip circuit breaker (unit tested)
</verification>

<success_criteria>
- Integration tests prove AgentBackend trait works through MockBackend (all three modes)
- Dynamic dispatch (Box&lt;dyn AgentBackend&gt;) compiles and works
- All three provider handles reject missing API keys at construction
- Full workspace test suite green
- Human has reviewed the complete agent layer
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-clients/02-03-SUMMARY.md`
</output>
